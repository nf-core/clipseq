nextflow_pipeline {
    name "genome_gtf"
    script "main.nf"

    test("genome_gtf_raw") {
        tag "genome_gtf_raw"
        tag "test"
        when {
            params {
                outdir = "$outputDir"
                only_genome = true
                save_reference = true
                gtf = "s3://ngi-igenomes/igenomes/Saccharomyces_cerevisiae/Ensembl/R64-1-1/Annotation/Genes/genes.gtf"
            }
        }

        then {
            assert workflow.success

            // There is nothing to directly test
        }
    }

    test("genome_gtf_gz") {
        tag "genome_gtf_gz"
        tag "test"
        when {
            params {
                outdir = "$outputDir"
                only_genome = true
                save_reference = true
                gtf = "assets/reference/hg38_chr21.gtf"
            }
        }

        then {
            assert workflow.success

            // There is nothing to directly test
        }
    }

    test("genome_gtf_filt") {
        tag "genome_gtf_filt"
        tag "test"
        when {
            params {
                outdir = "$outputDir"
                only_genome = true
                save_reference = true
                gtf = "assets/reference/hg38_chr21.gtf"
                filtered_gtf = null
            }
        }

        then {
            assert workflow.success

            assert file("$outputDir/00_genome/hg38_chr21_representative_transcript_filtered.gtf").exists()
        }
    }

    test("genome_gtf_filt_seg") {
        tag "genome_gtf_filt_seg"
        tag "test"
        when {
            params {
                outdir = "$outputDir"
                only_genome = true
                save_reference = true
                gtf = "assets/reference/hg38_chr21.gtf"
                seg_filt_gtf = null
            }
        }

        then {
            assert workflow.success

            assert file("$outputDir/00_genome/hg38_chr21_representative_transcript_filtered_regions.gtf.gz").exists()
            assert file("$outputDir/00_genome/hg38_chr21_representative_transcript_filtered_seg.gtf").exists()
        }
    }

    test("genome_gtf_seg") {
        tag "genome_gtf_seg"
        tag "test"
        when {
            params {
                outdir = "$outputDir"
                only_genome = true
                save_reference = true
                gtf = "assets/reference/hg38_chr21.gtf"
                seg_gtf = null
            }
        }

        then {
            assert workflow.success

            assert file("$outputDir/00_genome/hg38_chr21_regions.gtf.gz").exists()
            assert file("$outputDir/00_genome/hg38_chr21_seg.gtf").exists()
        }
    }

    test("genome_gtf_resolve_unanno") {
        tag "genome_gtf_resolve_unanno"
        tag "test"
        when {
            params {
                outdir = "$outputDir"
                only_genome = true
                save_reference = true
                gtf = "assets/reference/hg38_chr21.gtf"
                seg_resolved_gtf = null
            }
        }

        then {
            assert workflow.success

            assert file("$outputDir/00_genome/hg38_chr21_representative_transcript_filtered_seg_genicOtherfalse.resolved.gtf").exists()
        }
    }

    test("genome_gtf_resolve_unanno_region") {
        tag "genome_gtf_resolve_unanno_region"
        tag "test"
        when {
            params {
                outdir = "$outputDir"
                only_genome = true
                save_reference = true
                gtf = "assets/reference/hg38_chr21.gtf"
                regions_resolved_gtf = null
            }
        }

        then {
            assert workflow.success

            assert file("$outputDir/00_genome/hg38_chr21_representative_transcript_filtered_regions_genicOtherfalse.resolved.gtf").exists()
        }
    }

    test("genome_gtf_resolve_unanno_genic") {
        tag "genome_gtf_resolve_unanno_genic"
        tag "test"
        when {
            params {
                outdir = "$outputDir"
                only_genome = true
                save_reference = true
                gtf = "assets/reference/hg38_chr21.gtf"
                seg_resolved_gtf_genic = null
            }
        }

        then {
            assert workflow.success

            assert file("$outputDir/00_genome/hg38_chr21_representative_transcript_filtered_seg_genicOthertrue.resolved.gtf").exists()
        }
    }

    test("genome_gtf_resolve_unanno_region_genic") {
        tag "genome_gtf_resolve_unanno_region_genic"
        tag "test"
        when {
            params {
                outdir = "$outputDir"
                only_genome = true
                save_reference = true
                gtf = "assets/reference/hg38_chr21.gtf"
                regions_resolved_gtf_genic = null
            }
        }

        then {
            assert workflow.success

            assert file("$outputDir/00_genome/hg38_chr21_representative_transcript_filtered_regions_genicOthertrue.resolved.gtf").exists()
        }
    }
}
